package com.example.hotelroll.data.model

// Class meant to show if room is available for reservation assignment (not checking time conflicts but
// room condition in general
enum class RoomStatus {
    AVAILABLE,
    UNAVAILABLE,
}